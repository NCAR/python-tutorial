version: 2.1

jobs:

  build_site:

    docker:
      - image: circleci/python:latest

    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "3c:34:9e:23:fa:13:a4:d9:01:fb:dd:54:a4:99:28:f6"
      - restore_cache:
          key: deps1-{{ .Branch }}-{{ checksum "conf.py" }}
      - run:
          name: Install Nikola
          command: sudo pip install nikola[extras]
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "conf.py" }}
          paths:
            - "$HOME/.cache/pip"
      - run:
          name: Build site with Nikola
          command: nikola build
      - store_artifacts:
          path: ./output/

  deploy_site:

    docker:
      - image: circleci/python:latest

    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "3c:34:9e:23:fa:13:a4:d9:01:fb:dd:54:a4:99:28:f6"
      - restore_cache:
          key: deps1-{{ .Branch }}-{{ checksum "conf.py" }}
      - run:
          name: Install Nikola
          command: sudo pip install nikola[extras]
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "conf.py" }}
          paths:
            - "$HOME/.cache/pip"
      - run:
          name: Build site with Nikola and deploy to GitHub Pages
          command: |
            git config --global user.name 'Xdev Bot'
            git config --global user.email '47308153+xdev-bot@users.noreply.github.com'
            nikola build
            nikola github_deploy -m '[ci skip] Nikola auto deploy'

workflows:
  version: 2
  build:
    jobs:
      - build_site:
          filters:
            branches:
              ignore:
                - gh-pages
                - master

      - deploy_site:
          filters:
            branches:
              only:
                - master
              ignore:
                - gh-pages
